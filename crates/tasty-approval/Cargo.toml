[package]
name = "tasty-approval"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Human-handoff approval workflow: request/respond/await with timeout + waiter channels"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[lints]
workspace = true
