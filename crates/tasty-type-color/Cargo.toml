[package]
name = "tasty-type-color"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "GPU-ready color primitives (GpuRgba/GpuRgb newtypes) for Tasty. Leaf crate — must not depend on any other tasty-* crate."

[dependencies]
bytemuck = { version = "1", features = ["derive"] }

[lints]
workspace = true
