[package]
name = "tasty-output"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Semantic output parsers for tasty surfaces (path/url/prompt/exit-code...)"

[dependencies]
regex = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1"

[lints]
workspace = true
