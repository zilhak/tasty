[package]
name = "tasty-plugin-agent-common"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Shared helpers for the AI-CLI child-manager plugins (claude, codex). Library crate — no tasty-plugin.toml, so it is not a bundled plugin."

[dependencies]
tasty-plugin-sdk = { path = "../tasty-plugin-sdk" }
serde_json = "1"
tracing = "0.1"

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
