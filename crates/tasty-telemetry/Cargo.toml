[package]
name = "tasty-telemetry"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Agent telemetry: metric recording, rollup, summary, timeseries (Phase 4)"

[dependencies]
tasty-memory = { path = "../tasty-memory" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[lints]
workspace = true
