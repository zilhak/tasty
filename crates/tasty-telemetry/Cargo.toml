[package]
name = "tasty-telemetry"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Agent metric recording, aggregation, cost limits, and anomaly signals"

[dependencies]
tasty-memory = { path = "../tasty-memory" }
tasty-utils = { path = "../tasty-utils" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[lints]
workspace = true
