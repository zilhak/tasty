[package]
name = "tasty-hooks"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Event matching and hook bindings for host-side execution"

[dependencies]
regex = "1"
serde = { version = "1", features = ["derive"] }
tracing = "0.1"

[lints]
workspace = true
