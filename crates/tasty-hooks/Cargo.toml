[package]
name = "tasty-hooks"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Event-based shell command hook system"

[dependencies]
regex = "1"
serde = { version = "1", features = ["derive"] }
