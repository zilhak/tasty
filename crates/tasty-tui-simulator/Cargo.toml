[package]
name = "tasty-tui-simulator"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "VTE sequence simulator for testing tasty terminal emulation"

[[bin]]
name = "tasty-tui-sim"
path = "src/main.rs"

[dependencies]
crossterm = "0.28"
clap = { version = "4", features = ["derive"] }
