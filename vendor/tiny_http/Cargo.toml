# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2018"
name = "tiny_http"
version = "0.12.0"
authors = [
    "pierre.krieger1708@gmail.com",
    "Corey Farwell <coreyf@rwell.org>",
]
description = "Low level HTTP server library"
documentation = "https://tiny-http.github.io/tiny-http/tiny_http/index.html"
readme = "README.md"
keywords = [
    "http",
    "server",
    "web",
]
license = "MIT OR Apache-2.0"
repository = "https://github.com/tiny-http/tiny-http"

[package.metadata.docs.rs]
features = ["ssl-openssl"]

[dependencies.ascii]
version = "1.0"

[dependencies.chunked_transfer]
version = "1"

[dependencies.httpdate]
version = "1.0.2"

[dependencies.log]
version = "0.4.4"

[dependencies.openssl]
version = "0.10"
optional = true

[dependencies.rustls]
version = "0.20"
optional = true

[dependencies.rustls-pemfile]
version = "0.2.1"
optional = true

[dependencies.zeroize]
version = "1"
optional = true

[features]
default = []
ssl = ["ssl-openssl"]
ssl-openssl = [
    "openssl",
    "zeroize",
]
ssl-rustls = [
    "rustls",
    "rustls-pemfile",
    "zeroize",
]

# tasty: 상류 0.12.0 소스가 이미 내는 경고(미사용 re-export · 쓰이지 않는 dummy trait)다.
# path 의존은 경고 상한이 안 걸려 모든 빌드에 뜨므로 끈다 — 소스를 상류와 같게 두려고
# 소스가 아니라 여기서 끈다.
[lints.rust]
unused = "allow"
