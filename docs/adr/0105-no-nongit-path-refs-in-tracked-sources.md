# ADR-0105: 추적되는 파일에는 git 에 없는 경로를 적지 않는다 — 로컬 작업 폴더의 위치는 로컬 지침이 정한다

- **Status**: Accepted
- **Date**: 2026-09-04
- **Tags**: docs, conventions, hygiene, dead-reference, gitignore, local-workspace, guard-test, adr-0027, adr-0096

## Context

에이전트가 쓰는 로컬 작업 폴더(임시 산출물 · 작업 계획 · 번호 붙은 작업 티켓 · 로컬 전용
지침)는 `.gitignore` 대상이다. 커밋되지 않고, 완료된 항목은 관례상 파일 자체가 삭제되며,
번호는 다음 세션에서 재사용된다. 그런데 커밋되는 파일 쪽에서 그 경로를 좌표로 적어 온
관행이 있었다. 저장소를 새로 clone 한 사람에게 그 좌표는 **존재한 적이 없고**, 번호 재사용
탓에 살아 있는 것처럼 보이면서 *무관한 문서* 를 가리키기까지 한다. 죽은 참조를 넘어
오도하는 참조다. ADR-0027 이 Figma 기획 기록에서 같은 증상을 "세션/트랙 식별자 누수" 로
이미 진단했다.

**이전 규정 상태는 두 갈래였고 둘 다 좁았다.**

| 규정 | 범위 | 한계 |
|---|---|---|
| [`template.md`](template.md) "외부(비-git) 위치 문서 참조 금지" | ADR 및 committed 문서가 비-git 위치의 *문서를 SoT 로 참조/링크* 하는 것 | **예외**: "폴더의 존재·용도(컨벤션) 자체를 정의/설명하는 서술은 허용" |
| 루트 `CLAUDE.md` "소스 주석의 TODO 파일 및 디자인 changelog 인용 금지" | 소스 주석·문자열의 *티켓 번호 인용* 과 디자인 changelog slug | 경로 서술 일반이 아니라 번호/slug 형태에 한정 |

그 예외는 어느 ADR 에도 근거가 없었다. 그리고 예외가 있는 한 누수는 계속됐다 — 임시 파일
기본 위치를 설명한다는 명목으로 절차 문서·개발 가이드·벤치 스크립트·README 에 로컬 경로가
스며들었다. 예외가 정확히 그 통로였다는 것이 실측으로 확인됐다.

같은 축의 선례가 [ADR-0096](0096-unit-tests-isolated-from-user-environment.md) 이다 —
커밋되는 산출물(그때는 테스트)이 로컬 머신 상태에 의존하지 않게 만든 결정이다. 이 ADR 은
같은 원칙을 *문서·소스에 적힌 좌표* 에 적용한다.

## Decision

**git 이 추적하는 모든 파일은 `.gitignore` 로 제외된 레포 로컬 작업 폴더의 경로를 적지
않는다.** 대상에는 코드·테스트·스크립트·`docs/`·루트 `CLAUDE.md`·README·CI 설정·plugin
매니페스트가 모두 포함된다. **폴더 이름을 단독으로 언급하는 것도 금지에 포함되며, 컨벤션
설명 예외는 이 ADR 로 폐지한다** — `template.md` 의 "폴더의 존재·용도 설명은 허용" 항목은
효력을 잃는다(문구 정리는 아래 "집행" 참조).

**대신 위임한다.** 추적 문서는 규칙의 *내용* 만 쓰고 *위치* 는 쓰지 않는다: "임시 산출물과
작업 계획은 프로젝트 루트나 소스 디렉토리에 만들지 않는다. 구체적 위치는 커밋되지 않는
로컬 전용 지침이 정한다." 실제 경로는 그 로컬 지침 파일에만 존재한다 — 그 파일 자신이
gitignored 이므로 clone 한 사람에게 죽은 좌표를 남기지 않는다.

**범위 밖(적어도 되는 것) 네 가지를 확정한다.**

1. **빌드가 생성·소비하는 gitignored 산출물** — `target/`, `dist/`, plugin 번들 서명 파일,
   사이트 생성물. 문서에 적힌 절차대로 빌드하면 clone 한 사람의 트리에도 *실제로 생긴다*.
   재현 가능한 좌표이므로 죽은 참조가 아니다.
2. **사용자 홈의 런타임 경로** — `~/.tasty/` 같은 데이터 루트나 외부 도구의 홈 설정 경로.
   레포 트리가 아니라 제품이 런타임에 읽고 쓰는 위치이며, 그 자체가 명세해야 할 동작이다.
3. **경로가 아닌 식별자** — plugin id, 워크스페이스 이름, 설정 키 등. 파일시스템 좌표가
   아니므로 이 규칙의 대상이 아니다.
4. **`.gitignore` 자신** — 제외 항목을 적는 것이 그 파일의 본질이다.

**툴링이 그 이름을 데이터로 보유해야 하는 경우는 "인용" 이 아니라 "입력" 이다.** 하이진
가드의 탐지 상수·순회 가지치기 목록, 경로 해석 테스트의 픽스처 문자열이 여기 해당한다.
지우면 검사·테스트가 검증하려던 것 자체가 사라진다. 이 파일들은 가드의 allowlist 로
관리하며, allowlist 에 넣는 판단 기준은 **"금지 형태를 담는 것이 그 파일의 본질인가"** 다.

**집행은 `crates/tasty-doc-guards/tests/no_todo_file_citation.rs` 가 한다.** 탐지 범위는 번호 인용·conductor
번호·경로 인용·changelog slug·앵커 슬러그·폴더 이름 단독 언급·산문 언급 일곱 형태이고, 스캔
대상은 레포 전체 파일이다(일곱째는 아래 "산문 언급까지 넓힌다" 절에서 더해졌다). `template.md` 의 "폴더의 존재·용도 설명은 허용" 예외도 함께 삭제됐다.

### 집행 형태 (집행 트랙에서 확정 — 2026-09-04)

아래는 위 결정을 실현하는 형태다. 결정 내용 자체는 바뀌지 않았다.

- **스캔 대상은 denylist 전수 순회로 정한다.** 확장자·디렉토리 화이트리스트로 "볼
  파일" 을 열거하지 않고, 순회가 닿는 모든 파일을 대상으로 삼되 바이너리 확장자와
  gitignored 디렉토리만 뺀다. 규칙 범위가 "git 이 추적하는 모든 파일" 이므로 대상
  집합도 열거가 아니라 배제로 정의해야 1:1 로 대응한다 — 화이트리스트는 스크립트·CI
  설정·루트 문서·공개 사이트 콘텐츠를 통째로 놓쳤고, 항목을 채워 넣어도 다음
  사각지대가 또 생긴다. `git ls-files` 로 추적 집합을 직접 묻는 방법은 채택하지
  않았다 — 테스트가 git 바이너리와 저장소 메타데이터의 존재에 의존하게 되어 tarball
  빌드에서 깨지고, ADR-0096 의 "테스트는 환경을 읽지 않는다" 와도 어긋난다.
- **예외는 파일 통째가 아니라 패턴 단위로 둔다.** 파일 전체를 면제하면 그 파일이
  *다른* 형태의 위반을 새로 들여도 영영 잡히지 않는다. 규칙 본문을 담은 문서일수록
  그렇게 되기 쉬워, 면제는 "이 파일에서 이 형태만" 으로 좁힌다. **가드 자신에게도
  면제를 두지 않는다** — 순회 입력으로 폴더 이름이 필요한 곳은 조각으로 조립하고,
  패턴 픽스처는 판정 지점을 끊어 쓴다. 가드 본인이 통째 면제의 유일한 예외로 남으면
  이 원칙이 성립하지 않는다. (이 항목의 뒷문장은 산문 언급에서 한 번 갈린다 — 아래
  "산문 언급까지 넓힌다" 절. 앞문장인 **패턴 단위 면제**는 그대로다.)
- **홈 경로 판별은 그 자리 직전 문맥으로 한다.** 범위 밖 2항(사용자 홈의 런타임 경로)은
  레포 로컬 폴더와 이름이 겹치므로 홈 표기를 근거로 통과시키되, 판정 범위는 **occurrence
  직전** 이다. 같은 줄 어디에나 홈 표기가 있으면 통과시키는 방식은, 정당한 홈 경로가 한 번
  나오는 줄에 섞인 진짜 레포 로컬 참조까지 함께 통과시킨다. 로컬 작업 폴더 쪽은 홈에
  존재할 수 없어 예외 자체를 두지 않는다.
- **매칭은 구분자 개수·대소문자로 회피되지 않아야 한다.** 구분자를 한 개만 소비하는
  매처는 공백을 하나 더 넣는 것만으로, 원문 그대로 비교하는 매처는 대문자 표기만으로
  통과한다. 규칙을 우회하려는 의도가 없어도 표기 차이로 새어 나가므로 구분자는 런으로
  소비하고 비교는 소문자로 한다. 단, 임의 문장부호까지 구분자로 보면 평범한 문장이
  걸리므로 번호 인용은 공백 런과 **짝 맞는 괄호 묶음**까지만 넓힌다. 괄호는 그 논거의
  반례가 아니다 — 여는 괄호 뒤 숫자에 닫는 괄호가 짝으로 따라올 것을 함께 요구하므로
  문장이 아니라 번호를 감싼 묶음만 걸린다.
- **vendored 서드파티 번들은 스캔에서 뺀다.** 수 MB 짜리 minified 라이브러리를 산문
  패턴으로 훑는 것은 순수 비용이고, 라이브러리를 갱신했을 때 그 안의 문자열이 우연히
  금지 형태를 띠면 무관한 이유로 CI 가 막힌다. 우리가 쓴 콘텐츠가 그 디렉토리에 들어가면
  가지치기를 좁힌다.

### 산문 언급까지 넓힌다 (2026-09-11)

위 여섯 형태는 전부 **번호나 경로가 붙은** 좌표였다. 실제로 남아 있던 누수는 그 둘이 없는
형태다 — 티켓 자체를 산문의 명사로 가리키는 것(`이 TODO 는 순수 구조 이관이라` ·
`TODO 문서 초기 서술과 달리` · `a separate TODO if needed`). 가리키는 대상이 커밋되지 않는
로컬 문서인 것은 같고, 번호가 없다고 덜 죽은 참조가 되지 않는다. 갓 clone 한 사람에게는
숫자가 없는 쪽이 오히려 **무엇을 가리키는지 물을 좌표조차 없다.** 실측 2026-09-11 로 번호
없는 형태가 코드·스크립트·WIT·JSX 에 20 건 살아 있었고, 그중 15 건은 P1 이 옆을 지나가며
통과시킨 자리였다.

- **판정선은 콜론이다.** 대문자 `TODO` 바로 뒤에 `:` 또는 `(<범위>):` 가 오면 **할 일
  표시**로 보고 통과시키고, 그 외의 모든 출현을 위반으로 잡는다. 티켓을 가리키는 산문은
  `TODO` 를 문장 안의 명사로 쓰고, 할 일 표시는 그 뒤에 곧바로 콜론을 붙여 설명을 연다 —
  둘을 가르는 표지가 그것 하나다. 괄호 형태는 **닫는 괄호 뒤에 콜론이 와야** 인정한다:
  괄호만 보면 `TODO(…)` 로 끝나는 산문이 그대로 샌다. 할 일 표시를 금지하지 않는 이유는
  이 ADR 이 막는 것이 *죽은 좌표* 이지 *미완 표시* 가 아니기 때문이다 — 후자는 가리키는
  대상이 없으므로 clone 한 사람에게 해소할 참조 자체가 없다.
- **마크다운은 범위 밖이다.** 문서는 규칙 본문을 인용하는 것이 자기 일이고(이 가드가
  무엇을 금지하는지 적으려면 그 형태를 그대로 적어야 한다), 상류 라이브러리가 자기 소스에
  남긴 마커를 서술하는 자리도 문서다. 코드 주석에는 그 일이 없다 — 티켓을 가리키는 것
  말고 `TODO` 를 산문으로 쓸 이유가 없다. 이 배제는 `ALLOWLIST` 가 아니라 **범위 축**으로
  둔다: 면제로 흉내 내면 `.md` 파일 수만큼 항목이 늘고 새 문서마다 항목을 더해야 한다.
  면제는 *그 파일 하나* 가 금지 형태를 담는 것이 본질일 때 주는 것이고, 범위는 *확장자
  전체* 에 대해 그 패턴이 애초에 물음이 아닌 경우다.
- **가드 자신은 이 형태 하나만 면제받는다.** 위 "예외는 파일 통째가 아니라 패턴 단위"
  항목의 뒷문장("가드 자신에게도 면제를 두지 않는다")이 여기서 한 번 갈린다. 번호가 붙는
  형태는 조각으로 끊어 쓸 구분자가 있었지만 산문 언급에는 없다 — 판정 대상이 `TODO` 라는
  **낱말 자체**라 끊을 자리가 없다. 원칙의 본체(패턴 단위 면제)는 그대로다: 가드 파일도
  P7 만 면제이고 P1·P3·P6 은 그대로 잡힌다. 같은 이유로 ADR-0037 의 규칙 본문(**빈
  사유·"TODO" 금지**)을 인용하는 `scripts/check-allow-reason.sh` 도 P7 만 면제한다.
- **커밋 시점 채널이 붙었다.** `.githooks/pre-commit` 의 `T.1` 이 이 가드를 부른다. 그
  훅의 다른 검사들과 달리 **레포 전체 작업 트리**를 본다(가드의 좌변이 순회다). 비용은
  실측 2.0 s 이고 무관한 소스를 고친 뒤에도 같다 — `tasty-doc-guards` 는 의존이 0 이라
  워크스페이스가 바뀌어도 재빌드하지 않는다. pre-push `B.7` 과 겹치는 것은 의도다:
  두 훅은 커밋과 push 라는 서로 다른 자리를 막는다.

## Consequences

- **얻은 것**: clone 한 사람 기준으로 추적 파일의 모든 경로 참조가 해소 가능해진다. 규칙이
  "어떤 형태의 인용인가" 가 아니라 "그 경로가 git 에 있는가" 하나로 판정되므로, 새로운
  누수 형태(경로 서술·기본값 안내·스크립트 기본 인자)가 나와도 같은 기준이 그대로 적용된다.
- **얻은 것**: 정책과 위치가 분리된다. 로컬 폴더 배치를 바꿔도 추적 문서를 고칠 일이 없다 —
  로컬 지침 한 곳만 고치면 된다.
- **잃은 것**: 추적 문서만 읽어서는 임시 파일을 *어디에* 두는지 알 수 없다. 로컬 지침을
  함께 로드하지 않는 독자(웹에서 레포만 보는 사람)에게는 한 단계 간접이 생긴다. 그 대가로
  얻는 것이 "적힌 경로는 전부 실재한다" 는 보장이라, 간접 쪽을 택했다.
- **잃은 것**: 이 ADR 본문 자신도 규칙을 지켜야 해서 폴더 이름을 적지 못한다. 그래서 서술이
  "레포 로컬 작업 폴더" 같은 기술(descriptive) 표현이 된다 — 규칙의 자기 적용이므로 의도된
  결과다.
- **얻은 것 (2026-09-11)**: 번호·경로가 안 붙은 산문 언급까지 기계가 판정한다. 판정선이
  콜론 하나라 "이것은 티켓 인용인가 할 일 표시인가" 를 사람이 매번 고르지 않아도 된다.
- **잃은 것 (2026-09-11)**: 코드 주석에서 `TODO` 를 일반 명사로 쓸 수 없다 — "이 TODO 는"
  대신 그 자리에서 이유를 서술해야 한다. 그것이 이 ADR 이 원하던 결과라 대가로 친다.
  대신 `.md` 가 범위 밖이라 규칙을 *설명하는* 글에는 제약이 없다.
- **운영 비용 / 유지 부담**: 가드가 확장되기 전까지는 사람·에이전트 판단에 의존한다. 그
  기간에 새 위반이 유입될 수 있다. 또 allowlist 는 늘어나기만 하는 목록이라, 항목마다
  "본질인가" 를 다시 묻지 않으면 우회 통로가 된다.

## Alternatives Considered

- **컨벤션 설명 예외를 유지한다** — 가장 작은 변경이지만, 이번 위반 목록이 곧 그 예외가
  통로였다는 증거다. "폴더의 용도를 설명한다" 와 "그 폴더의 경로를 좌표로 쓴다" 는 실제
  문장에서 구분되지 않았다.
- **금지 대신 "존재하면 경고" 수준의 권고로 둔다** — 권고는 이미 두 규정으로 존재했고
  누수를 막지 못했다. 판정이 이분법이어야 기계가 강제할 수 있다.
- **로컬 작업 폴더를 git 추적 대상으로 바꾼다** — 참조가 살아나긴 하지만, 세션마다 생기고
  지워지는 티켓·임시 산출물이 히스토리에 영구히 쌓인다. 폴더를 gitignored 로 두는 이유
  자체를 없애는 선택이라 기각. (다만 이것이 재검토 트리거다.)
- **레포 안에 추적되는 별칭 문서를 두고 그것만 참조한다** — 별칭이 실제 위치와 어긋나는
  순간 같은 드리프트가 재발한다. SoT 를 하나로 두는 편(로컬 지침)이 낫다.
- **범위를 `docs/` 로만 한정한다** — 실제 위반의 절반 이상이 스크립트·테스트·README 에
  있었다. 문서만 정리하면 같은 좌표가 코드 쪽에 남는다.

## Reconsideration Triggers

다음 중 하나가 충족되면 본 ADR 을 재검토한다.

- 로컬 작업 폴더가 git 추적 대상으로 바뀐다 — "clone 에 존재하지 않는다" 는 전제가 깨져
  금지 근거가 사라진다.
- 하이진 가드가 정당한 참조를 반복 오탐한다 — allowlist 가 계속 늘어난다면 판정 기준
  자체(경로 존재 여부)가 실무와 맞지 않는다는 신호다.
- 위임 구조가 실패한다 — 로컬 지침을 로드하지 않는 경로(CI, 외부 기여자)에서 임시 파일이
  레포 루트에 계속 생긴다면, 위치를 추적 문서에 되돌리는 대신 다른 전달 수단을 찾는다.
- 산문 언급(P7)의 콜론 판정선이 정당한 주석을 반복 오탐한다 — 예컨대 `TODO` 를 도메인
  용어로 쓰는 코드(할 일 목록 기능 자체)가 들어오면 판정선을 대문자·콜론이 아닌 다른
  축으로 옮겨야 한다. 재는 법: 그 가드가 빨개진 자리를 모아, 가리키는 대상이 커밋되는
  문서인 건수를 센다. 0 이 아니면 이 트리거가 켜진 것이다.
- "범위 밖" 네 항목 중 하나가 실제로 죽은 참조를 만든다 — 예컨대 빌드 산출물 경로가
  프로필마다 달라져 문서에 적힌 좌표가 재현되지 않게 되는 경우.

## References

- [`template.md`](template.md) — "외부(비-git) 위치 문서 참조 금지" 작성 규칙. 이 ADR 이 그
  예외 조항을 폐지한다.
- [ADR-0027](0027-figma-planning-sot-naming-derived-index.md) — 같은 증상을 Figma 기획
  기록에서 "세션/트랙 식별자 누수" 로 진단한 선례. 이 ADR 이 0027 의 결정을 바꾸지는 않는다.
- [ADR-0096](0096-unit-tests-isolated-from-user-environment.md) — 커밋되는 산출물이 로컬
  머신 상태에 의존하지 않게 한 같은 축의 결정.
- 루트 `CLAUDE.md` "소스 주석의 TODO 파일 및 디자인 changelog 인용 금지" — 이 ADR 이 넓힌
  규칙의 좁은 선행 규정.
