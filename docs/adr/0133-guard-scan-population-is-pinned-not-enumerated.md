# ADR-0133: 소스 스캔 가드의 모수는 열거하지 말고 고정한다

- **Status**: Accepted
- **Date**: 2026-09-04
- **Tags**: guards, design-tokens, testing, ci, adr-0126, adr-0128, adr-0033

## Context

레포에는 소스를 런타임에 읽어 금지 형태를 찾는 가드가 **많다.** 세었다 — 레포 루트를
`env!("CARGO_MANIFEST_DIR")` 로 잡고 `read_to_string`/`read_dir` 로 소스를 읽는 추적
파일이 **50 개**이고, 그중 **14 개**가 경로 목록 상수(`*_ROOTS` / `*_DIRS` / `SCANNED`)를
선언한다.

```bash
# 모수 = 추적 `.rs` 전체. base = 이 ADR 을 쓴 lane 의 베이스 `d7dc4079`.
for f in $(git ls-files '*.rs'); do
  grep -q CARGO_MANIFEST_DIR "$f" && grep -qE 'read_to_string|read_dir' "$f" && echo "$f"
done | wc -l                                    # 50
# 그중 경로 목록 상수를 가진 것
... grep -qE 'const [A-Z_]*(ROOTS|DIRS|SCAN)[A-Z_]*: &\[&str\]' ...   # 14
```

이런 가드는 둘로 이루어진다.

1. **판정** — 무엇을 위반으로 볼 것인가 (접두 목록, 정규식, 값 비교).
2. **모수** — 어떤 파일을 읽을 것인가 (`SCAN_ROOTS` 같은 경로 목록).

지금까지의 논의와 리뷰는 거의 전부 ①에 몰려 있었다. 접두를 하나 빠뜨렸다는 것은
**위반이 통과하는 형태로** 드러나기 때문이다. 반면 ②는 다르게 실패한다.

### 실측 — 모수가 좁아 위반 넷이 조용히 살았다

세 스캔 루트가 `src/gfx/gpu` 를 디렉토리가 아니라 `shell_setup.rs` **한 파일**로 등재하고
있었다. 그 뒤 같은 디렉토리에 `boot_error.rs` 가 생겼고, **이미 등재돼 있던 접두**
(`Stroke::new(`)를 쓰는 리터럴 둘과 반경 리터럴 둘, 합 넷을 들여왔다. 가드는 그동안 계속
초록이었다.

**판정에는 아무 결함이 없었다.** 같은 파일을 모수에 넣자마자 넷 다 즉시 잡힌다. 결함은
전적으로 모수 쪽이었고, 모수는 **자기가 무엇을 안 보는지 말하지 않는다.**

변이로 양쪽 조건에서 확인했다(한쪽만 재면 "넓히니 잡힌다" 까지이고 "좁으면 놓친다" 는
연역이 된다).

```text
모수 = src/gfx/gpu        + 위반 심음 → 잡힌다
모수 = shell_setup.rs 한 파일 + 같은 위반 → 통과한다 (같은 판정, 같은 접두)
```

### 같은 축의 다른 얼굴 — 건수를 세 번 틀리게 셌다

같은 반경 축의 건수가 세 번 다르게 보고됐다. 3 → 7 → 9 다.

- **3**: `.corner_radius(` 뒤에 숫자가 바로 오는 형태만 봤다. 다수가
  `.corner_radius(egui::CornerRadius::same(12))` 로 감싸여 있었다. **판정의 결함.**
- **7**: 판정을 고친 뒤 다시 셌는데 모수를 가드의 `SCAN_ROOTS` 로 잡았다.
  `boot_error.rs` 가 그 목록에 없어 둘이 빠졌다. **모수의 결함.**
- **9**: 모수를 추적 파일 전체로 두고 고친 판정으로 센 값.

**패턴을 고쳐도 모수가 틀리면 다시 적게 센다.** 그리고 두 번째 오류는 첫 번째를 고치는
그 작업 안에서 났다 — 판정에 주의를 쏟는 동안 모수는 검토 대상으로 올라오지도 않았다.

### 같은 결함을 다른 가드가 이미 겪었다 — 독립 사례

이 ADR 을 쓰면서 다른 가드의 주석에서 **같은 실패의 기록**을 찾았다.
`tests/ipc_router_table_parity.rs` 는 라우터 소스를 파일로 열거하다가 정확히 같은 일을
겪고 디렉토리 순회로 바꿨다. 그 파일의 주석이 그대로 적고 있다.

> `src/app/ipc/` 는 dispatch 스텝이 모여 있어 파일이 늘어도 사람이 `ROUTER_SOURCES` 에
> 손으로 추가하는 걸 잊으면 사각지대가 생긴다 — `window_required.rs` 가 실제로 그렇게
> 빠져 6 메서드가 [판정 밖으로 나갔다]

**축도 다르고(IPC 라우터 정합) 저자도 다른데 실패 형태가 같다.** 한 가드의 사고가 아니라
파일 열거라는 형태 자체의 성질이다.

### 일반 명제

**추가는 관측되고 누락은 관측되지 않는다.**

접두를 하나 더 넣으면 그 즉시 위반이 뜨거나 안 뜬다 — 어느 쪽이든 신호다. 모수를 하나
빠뜨리면 아무 일도 일어나지 않는다. 이 비대칭은 이 레포의 다른 곳에서도 같은 모양으로
나왔다. 스캔 대상 파일 수를 **하한**(`MIN_SCANNED_FILES` 같은 최소치)으로 박은 가드는
실제 모수가 그보다 크면 그 차이만큼 사각을 갖고도 초록이고, **건수 고정**은 그보다 낫지만
어떤 파일이 빠졌는지는 여전히 말하지 않으며, **집합 동등**(스냅샷과 `BTreeSet` 비교)만이
빠진 이름을 이름으로 말한다.

## Decision

**소스 스캔 가드의 모수는 열거하지 말고 고정한다.** 구체적으로 셋이다.

### 1. 경로 목록은 디렉토리로 쓴다 — 개별 파일을 등재하지 않는다

`SCAN_ROOTS` 류에 `dir/file.rs` 를 넣지 않는다. 그 디렉토리에 나중에 생기는 파일이
**기본 제외**가 되기 때문이다. 등재된 파일 하나만 검토받고 형제는 아무 판정도 받지 않는
상태가 만들어진다.

디렉토리 전체가 부담스러우면 **넓힌 비용을 먼저 재고** 결정한다 — 넓혔을 때 새로 걸리는
건수가 곧 비용이다. 0이면 공짜이므로 넓힌다. 0이 아니면 그 건수가 곧 "지금까지 안 보던
위반" 이고, 이식을 먼저 하고 넓힌다. 정말로 일부를 빼야 한다면 파일을 골라 넣는 것이
아니라 **면제를 명시**한다 — 면제는 목록에 남아 검토받지만 미등재는 남지 않는다.

### 2. 모수의 형태를 테스트로 고정한다

경로 목록이 위 형태를 지키는지를 가드 자신이 확인한다. 되돌리는 편집이 조용히 통과하지
않게 하는 것이 목적이고, 그 테스트가 없으면 되돌림은 diff 에서 한 줄로 보일 뿐이다.
`tests/design_token_adherence.rs` 의 `the_gpu_scan_root_is_a_directory_not_a_file` 이 그
형태다 — 루트가 디렉토리인지, 그리고 **그 루트가 실제로 파일을 걷어 오는지**를 함께 본다
(경로가 틀리면 예외가 아니라 조용히 0이 되고, 0인 모수는 언제나 초록이다).

### 3. 개수로 모수를 고정할 때는 하한이 아니라 집합 동등으로

**하한에는 용도가 둘 있고 하나만 나쁘다.**

- **연기 검사로서의 하한** — "경로가 틀렸거나 읽기에 실패했다" 를 잡는 용도. 이건 옳다.
  경로 오타는 예외가 아니라 **조용한 0** 을 만들고, 0 인 모수는 언제나 초록이기 때문이다.
  현재 `MIN_SCANNED_FILES` 셋(`src/design_token_guard.rs` 200 · `src/source_guards.rs` 900 ·
  `tests/ci_channel_claims_match_workflows.rs` 400)이 전부 이 용도이고 각자 doc 에 그렇게
  적혀 있다. 남긴다.
- **모수 고정으로서의 하한** — "이만큼 봤으니 다 봤다" 의 근거로 쓰는 용도. 이건 쓰지
  않는다. 실제 모수가 하한보다 크면 그 차이만큼 사각을 갖고도 초록이다.

모수를 고정해야 하면 **집합 동등**으로 한다 — 빠진 것과 새로 생긴 것을 이름으로 말해
준다. 건수 고정은 그 사이의 절충이다. 셋 중 무엇을 썼는지, 그리고 **어느 용도인지**를
가드 문서에 적는다.

**두 가드가 같은 축을 보면 그 둘의 모수가 같다는 것도 고정 대상이다.**
`src/design_token_guard.rs` 와 `tests/design_token_adherence.rs` 는 같은 축을 보고 doc 에
"갈라지면 안 된다" 고 적혀 있었는데 **실제로 갈라졌고 그동안 양쪽 다 초록이었다.** 상수를
공유할 수 없으므로(통합 테스트 아이템은 본체 crate 에서 안 보인다) 한쪽이 다른 쪽 소스를
읽어 대조한다 — `the_two_sister_guards_scan_the_same_roots` 가 그것이다.

## Consequences

- **얻은 것**: 새 UI 파일이 기본 포함이 된다. 지금까지는 파일이 생길 때 누군가 스캔 루트를
  같이 고쳐야 했고, 그 절차는 어디에도 적혀 있지 않았으며 실제로 지켜지지 않았다.
- **얻은 것**: 모수 문제가 판정 문제와 같은 등급의 검토 대상이 된다. "접두 다 넣었나" 만
  묻던 리뷰가 "이 모수가 무엇을 안 보나" 를 함께 묻게 된다.
- **잃은 것**: 디렉토리 안에 스캔이 부적절한 파일이 섞이면 면제를 써야 한다. 파일을 골라
  넣는 것보다 손이 더 간다 — 그것이 이 결정이 사려는 것이다(면제는 남고 미등재는 안 남는다).
- **운영 비용**: 새 스캔 루트를 넣을 때 넓힌 비용을 한 번 재야 한다. 실제로는 명령 한 줄이고,
  그 값이 0이면 그대로 넣는다.
- **강제 범위 — 두 자리는 기계가 보고 나머지는 아무도 안 본다.** 위 ②는 그 가드 파일
  안에서만 성립한다. 기계가 보는 것은 디자인 토큰 축의 둘뿐이다:
  `the_gpu_scan_root_is_a_directory_not_a_file`(통합 타깃, 헤드리스 잡)과
  `the_two_sister_guards_scan_the_same_roots`(본체 crate 의 lib 유닛이라
  `cargo test --workspace --lib --bins` 로 **자동으로 돈다**). 그 둘 밖 —
  **나머지 12 개의 경로 목록이 개별 파일을 등재하는 것을 막는 판정은 없다.** 이 선언의 좌표를 못박는다: **모수 = 추적 `.rs` 전체,
  base = `d7dc4079`.** base 가 바뀌어 공통 검사가 생기면 이 문단은 만료된다.
  - 공통 검사로 뽑는 것은 **지금도 가능하다** — 가드가 "더 늘면" 이 아니다. 14 개는 이미
    충분히 많다. 안 뽑은 이유는 목록마다 성격이 달라서다: 스캔 대상 목록 · 면제 목록 ·
    고정 소스 목록이 같은 `&[&str]` 형태로 섞여 있어, 형태만 보고 일률로 막으면
    **면제 목록까지 막힌다**(면제에 파일을 쓰는 것은 이 ADR 이 권장하는 형태다).
    가르려면 각 목록의 **용도**를 이름이나 타입으로 드러내는 선행 작업이 필요하다.

## Alternatives Considered

- **A: 파일 단위 등재를 유지하고 "새 파일 추가 시 루트도 갱신" 을 문서화** — 가장 작은
  변경이다. 안 고른 이유: 실제로 그 절차가 지켜지지 않은 사례가 이 ADR 의 발단이고,
  지켜졌는지를 확인할 채널이 없다. 문서는 누락을 관측 가능하게 만들지 못한다.
- **B: 레포 전체를 모수로 두고 면제로만 좁힌다** — 누락이 원리적으로 불가능해진다.
  안 고른 이유: 가드마다 대상 계층이 실제로 다르다(색 가드는 갤러리를 의도적으로 뺀다).
  전체를 모수로 두면 면제 목록이 계층 목록의 여집합이 되어 오히려 읽기 어려워지고,
  "뚱뚱한 allowlist 는 없는 가드보다 나쁘다" 는 기존 판단과 충돌한다.
- **C: `git ls-files` 로 추적 파일을 모수로 삼는다** — 미등재가 사라지고 gitignore 도
  자동으로 존중된다. 안 고른 이유: 가드가 git 실행에 의존하게 되어 워크트리·CI·오프라인
  환경 차이를 새로 들인다. 현재 가드들은 `read_dir` + `env!("CARGO_MANIFEST_DIR")` 만 쓰는
  자족적인 형태이고 그 성질은 유지할 값이 있다. 다만 **사람이 건수를 셀 때**는 반대로
  `git grep`/`git ls-files` 가 옳다 — 셸의 `grep` 이 함수로 shadow 돼 `.gitignore` 를
  존중하는 환경이 있어, 무엇으로 셌는지를 수치와 함께 적어야 한다.

## Reconsideration Triggers

다음 중 하나가 충족되면 본 ADR 을 재검토한다.

> **이 절은 한 번 통째로 다시 썼다.** 처음에는 첫 항목이 **"소스 스캔 가드가 셋을 넘어
> 늘어난다"** 였는데, 세어 보니 작성 시점에 이미 50/14 였다 — **이미 충족돼 영원히
> 발화할 수 없는 조건**을 재검토 트리거라고 박아둔 상태였다. 트리거는 안 울려도 아무
> 신호를 내지 않으므로 그대로 두면 조용히 죽은 채 남는다.

- **경로 목록의 용도가 타입이나 이름으로 갈린다** — 스캔 대상 / 면제 / 고정 소스가 형태로
  구별되면 위 "강제 범위" 의 공통 검사를 만들 수 있다. 그 선행이 끝나는 것이 트리거다.
  (조건이 성립하면 달라지는 것: 12 개의 미보호 목록이 기계 판정 아래로 들어온다.)
- **디렉토리 모수가 감당 못 할 오검출을 만든다** — 한 가드의 면제 항목 수가 그 가드의
  스캔 루트 수를 크게 넘어서면 대안 B 가 더 읽기 쉬운 형태일 수 있다.
  (조건이 성립하면 달라지는 것: 모수를 레포 전체로 두고 면제로만 좁히는 형태로 뒤집는다.)
- **`MIN_SCANNED_FILES` 가 연기 검사 밖의 용도로 인용된다** — "N 개를 봤으니 다 봤다" 로
  쓰이는 자리가 생기면 그 가드는 집합 동등으로 올려야 한다.
  (조건이 성립하면 달라지는 것: 그 가드의 모수 고정 등급이 하한에서 집합 동등으로 바뀐다.)
- **가드의 실행 채널이 바뀌어 모수의 크기가 비용 문제가 된다**(현재는 전부 밀리초 단위다).
  (조건이 성립하면 달라지는 것: 디렉토리 순회의 비용을 재고 필요하면 대상을 좁힌다.)

## References

- [`docs/dev-guide/ci-gates.md`](../dev-guide/ci-gates.md) — 각 가드가 어느 잡에서 자동으로
  도는지의 정본. **모수가 맞아도 실행 채널이 없으면 결과는 같다** — 이 ADR 은 그 축과 독립이다
- [ADR-0126](0126-off-scale-font-values-are-not-snapped-to-tokens.md) — 스케일 밖 값의 처리.
  이 ADR 의 발단이 된 반경 축 정리가 그 규약을 따랐다
- [ADR-0033](0033-ui-color-semantic-role-only.md) — 색 가드의 모수가 갤러리를 의도적으로
  빼는 근거. "계층마다 모수가 다르다" 의 실례
- [ADR-0128](0128-dpi-conversion-guarded-by-source-scan-not-sealed-types.md) — 타입 봉인
  대신 소스 스캔 가드를 고른 결정. **그 가드도 자기 모수를 갖는다** — 이 ADR 의 ①②가
  그쪽에도 그대로 걸린다
- `tests/design_token_adherence.rs` — `SCAN_ROOTS` · `COLOR_SCAN_ROOTS` ·
  `GLYPH_SCAN_ROOTS` 와 `the_gpu_scan_root_is_a_directory_not_a_file`
